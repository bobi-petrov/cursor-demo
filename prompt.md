Add **threaded task comments** to the TeamFlow workshop app: each task has an ordered list of comments. 

Before coding, use **subagents** where they help: **api-designer** to settle any new HTTP surface and error shape, **nextjs-developer** for App Router layout and Prisma vs client `fetch` flow, **code-reviewer** on the final diff. Use **skills** deliberately: **test-driven-development** for pure helpers under `lib/` (e.g. ordering or formatting), **frontend-design** constrained to the TeamFlow dashboard look (density, neutrals—no rebrand). Ship UI on the task detail path to list and create comments, with `npm run build` passing.

